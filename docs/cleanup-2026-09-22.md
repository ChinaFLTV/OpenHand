# 2026-09-22 冗余清理记录

## 审查依据

- 起点 `ed63ffea4`，工作区干净。
- 对 889 个 Dart 文件执行语义引用分析，对 157 个 Web 源文件检查入口可达性与重复函数体，并复核日志输出入口。
- 语义扫描的 144 个候选不能直接视为死代码：生成测试中的代码字符串、JSON 编码隐式调用、接口实现和平台条件导出都需要人工复核。

## 已删除与合并

- 删除 `HookExecutionResult` 包装，直接返回逐项执行结果。去掉无人读取的成功、失败、超时计数、拦截汇总与重复错误列表，保留执行顺序、拦截短路、消息明细和使用统计。
- 删除 `AiModelProxyStreamDispatch` 包装，直接传递流式响应；去掉普通代理结果中无人读取的模型、后端和耗时字段，保留真实调用统计。
- 删除 `OpenRouterSyncPhase` 枚举及无人读取的进度阶段、异常字段；界面仍由实际结果和捕获的异常显示完成或失败。
- 删除三种代理策略枚举中未使用的标签，继续使用界面的本地化文案。
- 删除决策模型回退常量、无用默认问题别名和仅由测试引用的默认问题映射；回归直接验证生产默认问题入口，去掉重复测试输入与常量数量断言。
- 删除模板展示对象和模板说明中未读取的 Prompt 目录副本，以及子进程日志结果中无人读取的 PID。未修改 Prompt 内容、实际模板加载路径或进程跟踪。
- 合并 Web 消息翻译四条分支中的重复状态构造；移除重复颜色注释并同步 Hook 文档。
- 删除仅验证 Flutter 自带 TweenAnimationBuilder、没有调用项目实现的独立演示测试；保留实际动效组件回归。

## 保留依据

- 没有发现可直接删除的孤立源码文件。桌面条件导出和 `vite-env.d.ts` 均有实际用途。
- 私有声明可达性复查没有新增不可达声明；词法扫描剩余候选为运算符，不能按普通标识符引用规则删除。
- `silentLog` 在调试模式记录已处理异常；浏览器 `console.log` 用于录制协议；原生引擎标准输出包含启动握手。均保留。
- 不删除数据库迁移、隐式 JSON 序列化入口、有效业务测试，也不为相似的短回调增加碎片化函数。

## 验证

- Flutter 静态分析、Dart 格式检查、架构与共享辅助检查通过。
- 决策 25 项、消息与工作区 68 项、模型元数据 10 项、生命周期 5 项、实际动效组件 5 项回归通过；新增 Hook 检查实际执行子进程，验证失败继续、拦截停止及统计记录。
- 8 个模板清理前后的加载路径、系统装配文本、开发者指令和压缩指令逐字一致。
- `scripts/build_web.sh` 已执行，类型、运行时、交互、语音及产物校验通过。
- macOS 发布构建成功（241.7 MB）；内嵌 195 个 Web 文件与工作区产物 SHA-256 一致。

本轮没有修改 UI 布局与动效、数据库结构或原生 Rust 源码；Windows/Linux 未进行实机验收。仅提交本轮直接修改及 Web 重建产物，不推送。
