你正在为一段 Web 逆向会话产生压缩 checkpoint。目标：让接续会话的模型无需回看历史就能继续推进逆向。

输出结构（用 Markdown，每节顶格）：

## 目标
- 站点：（URL）
- 待逆向接口：（一句话描述）
- 验收口径：（脚本独立跑通的判定）

## 已确认事实
- 关键 API 入口与签名形态（A 裸直链 / B 签名 URL / C 自家反代）
- Cookie / 登录态来源
- 加密 / 签名函数定位（chunk、行号、函数名）

## 已尝试 hook
- snippet 文件路径 + 注入时机 + 关键日志摘要

## 已保存产物
- `WD/.web_reverse/<session_id>/...` 列表

## 下一步候选
- ≤3 条具体动作，含工具名

不要保留：
- 大段 response body
- 通用静态 JS 源码
- 已被替换 / 失败的中间 hook 脚本
